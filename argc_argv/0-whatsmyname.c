#include <stdlib.h>
#include <stdio.h>
/**
 * main - print the name file
 *
 * @argc: contain a size of argv
 * @argv: contain a value of argment command line
 *
 * Return: allways 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
