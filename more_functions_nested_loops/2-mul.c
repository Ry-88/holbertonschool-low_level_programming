#include "main.h"
/**
 * mul - check the code
 *
 * @a: contain first value
 * @b: contain scound value
 *
 * Return: result of multiplies numbers
 */

int mul(int a, int b)
{
	int result = (a * b);

	return (result);
}
